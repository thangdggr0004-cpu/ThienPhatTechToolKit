/**
 * Standard tags for system actions to enable advanced filtering.
 */
export enum ActionTags {
  WINDOWS = 'WINDOWS',
  OFFICE = 'OFFICE',
  SERVER = 'SERVER',
  KMS = 'KMS',
  BIOS = 'BIOS',
  NETWORK = 'NETWORK',
  REGISTRY = 'REGISTRY',
  DANGEROUS = 'DANGEROUS'
}
